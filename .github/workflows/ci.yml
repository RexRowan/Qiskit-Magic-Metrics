name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install package
        run: pip install -e ".[test,lint]"
      - name: Lint
        run: |
          ruff check .
          black --check .
      - name: Test
        run: pytest --cov=qiskit_magic_metrics --cov-report=term-missing

  # Placeholder job — enabled once qiskit_magic_metrics/lean/ has content (v0.2, see ROADMAP.md).
  # lean-verify:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Set up Lean
  #       run: echo "TODO: install elan / lake once lean/ has a lakefile"
  #     - name: Build and cross-check
  #       run: echo "TODO: lake build && python scripts/cross_check.py"
